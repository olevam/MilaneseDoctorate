import ClassificationQuery
import MapsQuery as MQ
#first is monastery (not origin!), second is doctype (if alone put number, - if all doctypes put None), third is classification (5 does all the leaders)
#ClassificationQuery.mon_classificaiton([38], (3,), 5)
MQ.loc_mon(38)